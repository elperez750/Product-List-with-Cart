export type DessertCardType = {
    image: string,
    category: string,
    name: string,
    price: number

}

export type CartItemType = {

    mealName: string,
    quantity: number,
    price: number

}